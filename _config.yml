plugins:
  - jekyll-remote-theme

remote_theme: http[s]://github.com/arunpariyar/techlabs-accelerator

defaults:
  - scope:
      path: ""
      type: "posts"
    values:
      layout: "post"
  - scope:
      path: ""
    values:
      layout: "default"

plugins:
  - jekyll-feed
  - jekyll-sitemap
  - jekyll-seo-tag

